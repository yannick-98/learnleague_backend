"""
Root conftest.py — shared pytest fixtures for the full test suite.
DJANGO_SETTINGS_MODULE is set via pytest.ini (config.settings.development).
pytest-django handles Django setup automatically.
"""
